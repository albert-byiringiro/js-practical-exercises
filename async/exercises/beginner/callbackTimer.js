/* 2. Create a function that takes a callback and executes it after 3 seconds using `setTimeout`.


function takeCall(callback){
  setTimeout(callback, 3000);
}

function remember(){
  console.log("Don't forget...");
}

takeCall(remember);

*/
